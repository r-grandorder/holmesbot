# Secrets — never commit
terraform/terraform.tfvars
terraform/.secrets/
.env
*.pem
*.tfstate
*.tfstate.*

# Terraform
**/.terraform/*
*.tfplan
crash.log

# Python (bot app, later)
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Local SQLite database (created by dbmate / the bot at runtime)
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal

data/servants.json
data/ce.json
scripts/data/community_aliases_review.txt

test_output/

# Custom-servant art staged for S3 (S3 is the source of truth; only the README is committed)
custom-assets/*
!custom-assets/README.md
